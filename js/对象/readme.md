# 对象
- 万物皆对象

1. 原始类型 string number boolean undefined null Symbol bigInt
2. 引用类型 function array object
- 创建对象的方式
1. 对象字面量
2. new Object()
3. new 调用自定义的构造函数

# 构造函数
1. 当我们需要批量化创建对象时，可以使用构造函数
2. 当一个函数被 new 调用时，我们称之为构造函数

# new 干了什么？
1. 创建一个 this 对象
2. 执行构造函数中的代码，就是给 this  对象添加属性值
3. 返回这个 this 对象

# 包装类
1. 当用户定义一个字面量，v8默认执行的还是 new Xxx()
2. 一个包装类的实例对象，在参与运算时，会自动拆箱成原始类型
3. 因为 js 是弱类型语言, 所以只有在赋值语句执行时才会判断值得类型(typeof)，当值被判定为原始类型时，就会自动将包装对象上添加的属性移除