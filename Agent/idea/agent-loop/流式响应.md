# 你跟claudeCode在终端说：“帮我把文件里的moment替换成dayjs”
1. 早期：一句话输入，白屏几秒后出现一段内容，然后工具调用，又白屏几秒，再出一段内容
2. 模型边输出，边调用工具

- 流式架构


# SSE （Server-Sent Events）  vs  WebSocket
 - WebSocket 全双工通信，客户端跟服务端建立连接后不释放，客户端可以随时发送消息给服务端，服务端也可以随时发送消息给客户端   （打视频电话）

 - LLM的流式输出，本质上是服务端向客户端一个一个的推送token，这个过程，客户端是不需要向服务端发送消息的

 - SSE：HTTP的一种特殊模式，客户端可以跟服务端建立一个长连接，服务端可以随时向客户端推送消息。SSE协议拥有重试机制，如果连接断开，客户端可以自动重连。


# Tool Call
 - 模型会输出一个 tool_use: []

  '{"file_'
  'path": "'
  'src/uti'
  'ls.ts"}'

 过早解读：调用错误的工具或者崩溃
 过晚解读：效率低


# 所有的工具都可以边说边调用？


# claudeCode 的并发安全判断
1. Read文件：只读不写，可以并发
2. Glob/Grep：只搜索不修改，可以并发
3. Edit 文件： 写入操作，必须串行
4. Bash 命令： ls(读取文件)可以并发， rm（删除文件）必须串行

# 工具执行的结果按什么顺序返回？
  claudeCode 会按工具调用的顺序返回结果，目的是为了保证工程的顺序性，可读性，方便调试
  类似与Promise.all() 顺序执行多个Promise，所有Promise执行完成后，才会返回结果，返回的顺序就是Promise调用的顺序


# 你说SSE是单向的，那你是怎么在SSE的过程中向Agent发审批yes的？
1. 流式输出：模型一个一个token往外蹦

2. 流式结束，弹出审批弹窗

3. 用户确认审批，执行工具调用  --- 你敲的yes，其实是发送了一个普通的HTTP请求

4. 新的SSE流 开始


审批其实是发生在两次SSE的间隙之间的不是发生在流式输出的过程中，用户在审批弹窗中点击yes，其实是发送了一个普通的HTTP请求，服务端收到请求后，会执行工具调用，然后返回结果给客户端



