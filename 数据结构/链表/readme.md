# 数组
1. 有序的，线性的，连续型的存储空间
2. 数组长度为 n，因为增加或者删除元素导致需要移动位置的元素数量就会随着 n 的值增大而增大，呈现了一个线性关系 时间复杂度为 O(n)
const arr = [1, 2, 3, 4]

const arr2 = ['hello', 1, #obj, #obj2] // 这种情况下 #obj，#obj2 对应的值不是连续的

- js 数组未必是真正的数组

3. 访问值 O(1)

# 链表
1. 有序的，线性的，离散型的存储空间
2. 增删一个结点不会带来其他结点的移动 O(1)
3. 访问值 O(n)
