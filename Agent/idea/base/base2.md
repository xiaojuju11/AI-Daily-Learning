# ChatGpt  vs  ClaudeCode（大模型和Agent的区别
- ChatGpt(大模型): 一问一答
- ClaudeCode: 一问，xxxxx ， 一答（读代码，分析代码，跑命令，该文件，跑测试，发现报错，修 bug，再跑测试，发现通过，执行代码走一套的循环逻辑）

- 重点就是谁在掌握---循环


# 三种形态
1. chatBot（最基础的形态）: 人发一条，Ai回一条
2. Copilot（类似代码提示补全Tab/Esc）: 人在工作，AI弹出建议，人选择是否接受，接收建议则执行
3. Agent：AI在开车，人坐副驾驶
    你：帮我把项目从 Express 迁移到 Hono
    AI：（开始思考）好，我需要先看一下现在的路由结构……
    AI：（读文件）读取 src/routes/index.ts
    AI：（分析）发现 15 个路由，3 个中间件……
    AI：（跑命令）pnpm add hono
    AI：（改文件）重写 src/routes/index.ts
    AI：（跑测试）pnpm test
    AI：（发现报错）有两个测试挂了，看看是什么问题……
    AI：（修 bug）修复类型不匹配的问题
    AI：（再跑测试）全部通过
    AI：搞定了，一共改了 8 个文件。

 人一句话，AI跑很多轮，每一轮都在做：想一步，做一步，看一步

 人一句话 -》 【think， Act， Observe 】 -》 完成，汇报


# ReAct 模式 
reasoning  + acting + Observation   想一步，做一步，看一步

- 一轮循环到底发生了什么

while () {
    1. 准备上下文   --- （
        检查上下文爆了吗，快爆了就压缩 -- 
            1. snip删掉旧的消息，2. Microcompact局部摘要，3. Auto-compact全局摘要）

    2. 调用模型 API  --- (模型流式返回，还在输出中，已经识别到的工具函数就要开始执行)
        边说边执行 -- 只有不冲突的操作才能并行  (并发)

    3. 决定是否继续  --- （claudeCode： 7种退出条件）
        1. 模型没有工具调用
        2. 流式传输被中断（要做前面的文字保留）
        3. 工具执行去中断
        4. hook阻止了继续执行
        5. 超过了最大轮数（防止死循环）
        6. 上下文过长，API拒绝
        7. 压缩后上下文还是过长，无法恢复


    4. 执行工具 --- （工具函数如果报错，错误信息就要写的足够优雅）

    5. 构建下一轮状态  --- 开始下一轮之前，处理一些杂活： 检查skill，记录已消费的命令...
}


# 状态追踪
1. 现在到第几轮了？ 用来判断是不是该停下 （保险丝）
2. 上一轮为什么选择了继续？（上一轮可能正常，也可能是错误恢复，也可能是压缩重试）
3. 压缩执行哪了？（是否触发紧急压缩，压缩后token下降多少）
4. 输出被截停了多少次？ （模型输出到一半被截停，一次恢复，两次恢复，三次就放弃）
5. 有没有被挂起的任务？ （）


# 实时反馈
Agent在执行的过程中，必须要将中间的结果展示给用户看 （边跑边说）

Generator(生成器)