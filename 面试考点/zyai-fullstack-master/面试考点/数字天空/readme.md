# 简单的说一下你简历的第一个项目
1. 为什么写这个项目：xxxxxx
2. 设计理念、特点：借助babel的编译能力 tsx =》 js
3. 难点：
    1. 资源的引入（将模块处理成 blob 本地地址）
    2. 通过 iframe 来预览最终的页面效果
    3. 对于代码编辑区域，做了代码提示，高亮处理
    4. 性能上：通过防抖来减少代码的编译次数

# 解释 @babel/standalone 完成 TSX/JSX 实时转译的过程
1. babel 会先将 tsx 编译成 AST 
2. 遍历该 AST，将 tsx 中的特殊语法转写成 js 语法，得到新 AST
3. 将新 AST 生成最终代码

# 我看你用了CSS Modules 和 SCSS，这两个你了解吗？有什么用？平常怎么样用的？
1. CSS Modules 样式隔离
2. SCSS （css 预处理器）变量，函数，嵌套，继承，模块化

# 平时用函数组件多还是类组件多？React 组件之间通信有哪些方式？
1. 父子
2. 子父
3. 兄弟： 上下文（context），仓库

# 我看你用 koa 做后端，简单介绍一下怎么用的
1. 定义路由接口
2. 封装了 jwt 的检验， 密码加密操作
3. 定义了控制层，处理前端的请求逻辑
4. 定义了模板层，处理跟数据库交互的逻辑


# 解释一下 axios 二次封装的过程。Ajax 请求怎么发送？
1. 定义了 超时时间， baseUrl， post请求期望的数据类型
2. 请求拦截：
3. 响应拦截： 1. 程序错误  2. 业务错误 

# indexedDB 是什么？有什么用？
1. 客户端数据库 （存非常大的数据，结构复杂的数据）
2. 先打开数据库，再创建对象仓库（表），最后操作数据