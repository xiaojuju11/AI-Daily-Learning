# KV Cache 、 Prompt Cache 、 context Collapse

1. KV Cache：模型推理层
 开发者不能控制


2. Prompt Cache： API层
 开发者能控制，模型提供商也提供了这样的计费优化

  {
    "model": "claude-sonnet-4-6",
    "max_tokens": 1024,
    "system": [
      {
        "type": "text",
        "text": "你是一个代码助手。以下是行为规则……（几千 token 的规则）",
        "cache_control": { "type": "ephemeral" }  // 👈 关键：标记缓存断点
      }
    ],
    "messages": [
      { "role": "user", "content": "帮我看看 auth.ts" }
    ]
  }


3. context Collapse
  开发者控制  
  与其将老消息直接删除，不如将它们全部折叠 -- 老消息存入外部存储，上下文里面只保留一个折叠标记
  


# 模型路由
1. 按任务类型，静态路由：xx类型的任务交给xx模型来干

2. 按难度，动态路由：需要设计一个分类器，根据用户当前的prompt来分析任务难度，简单任务交给便宜模型，复杂任务交给贵一点的模型